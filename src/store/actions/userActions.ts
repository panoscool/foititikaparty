import { asyncAction } from "../../utils/asyncAction"
import firebase from '../../config/firebase'

export const updateProfile = () => (dispatch: any) => {
  asyncAction(dispatch, async () => {

  })
}
